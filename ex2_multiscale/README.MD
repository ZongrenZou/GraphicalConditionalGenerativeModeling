## The multiscale Lorenz-96 example

[flow_distill_prune.py](flow_distill_prune.py): runs the method for the recovery of the graph with $m=10$.

[flow_distill_prune_multiple_dims.py](flow_distill_prune_multiple_dims.py): runs the method for $\Delta x_1$ with $m=20, 30, ..., 90, 100, 150, 200$.

[data/data.py](data.py): generates training data.
